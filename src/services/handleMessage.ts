// import { redisClient2 as subClient } from '../config';
// import
// (async function subscribe() {
//   try {
//       await subClient.subscribe(Environments.redis.channels.userCreatedProfileError, (err, count) => {
//           console.log(err);
//       });
//       await subClient?.subscribe(Environments.redis.channels.userCreatedMatchError, (err, ) => {
//           console.log(err);
//       });
//   } catch(err) {
//       console.error(enums.PrefixesForLogs.REDIS_SUBSCRIBE_CHANNEL_ERROR + err);
//   }
// })();
